# Copy to `.streamlit/secrets.toml` and fill in real values.
# `.streamlit/secrets.toml` is git-ignored. Never commit real credentials.
#
# Used only when running the Streamlit UI. If ANTHROPIC_API_KEY is also set
# in the environment, the environment value wins.

ANTHROPIC_API_KEY = "sk-ant-REPLACE_ME"
